{
  "dev": {
    "host": "{{API_HOST}}",
    "Security": {
      "Auth": {
        "oidc-paperless": {
          "Type": "OAuth2",
          "Grant Type": "Authorization Code",
          "Auth URL": "{{AUTH_URL}}",
          "Token URL": "{{TOKEN_URL}}",
          "Client ID": "{{OIDC_CLIENT_ID}}",
          "Client Secret": "{{OIDC_CLIENT_SECRET}}",
          "Scope": "openid profile email offline_access paperless_gpt",
          "Redirect URL": "{{REDIRECT_URL}}",
          "PKCE": true
        }
      }
    }
  },
  "prod": {
    "host": "{{API_HOST}}",
    "Security": {
      "Auth": {
        "oidc-paperless": {
          "Type": "OAuth2",
          "Grant Type": "Authorization Code",
          "Auth URL": "{{AUTH_URL}}",
          "Token URL": "{{TOKEN_URL}}",
          "Client ID": "{{OIDC_CLIENT_ID}}",
          "Client Secret": "{{OIDC_CLIENT_SECRET}}",
          "Scope": "openid profile email offline_access paperless_gpt",
          "Redirect URL": "{{REDIRECT_URL}}",
          "PKCE": true
        }
      }
    }
  },
  "prod-foreign-client": {
    "host": "{{API_HOST}}",
    "Security": {
      "Auth": {
        "oidc-paperless": {
          "Type": "OAuth2",
          "Grant Type": "Authorization Code",
          "Auth URL": "{{AUTH_URL}}",
          "Token URL": "{{TOKEN_URL}}",
          "Client ID": "{{OIDC_CLIENT_ID}}",
          "Client Secret": "{{OIDC_CLIENT_SECRET}}",
          "Scope": "openid profile email offline_access paperless_gpt",
          "Redirect URL": "{{REDIRECT_URL}}",
          "PKCE": true
        }
      }
    }
  }
}
